{
  "schemaVersion": 1,
  "baselineVersion": 35,
  "migrations": [
    {
      "version": 36,
      "name": "add_multi_currency_accounting",
      "categories": ["additive", "backfill", "constraint_tightening", "index"],
      "deployment": "online",
      "backfill": {
        "mode": "bounded",
        "resumable": true,
        "notes": "Each idempotent backfill visits only the finite set of pre-existing groups, expenses, ledgers, balances, or links and writes rows whose new value is missing. A failed transactional attempt can be retried without duplicating settings."
      },
      "locking": {
        "risk": "medium",
        "notes": "ALTER TABLE and foreign-key validation take bounded locks; existing-row scans and index builds must complete within the normal migration timeouts."
      },
      "rollback": {
        "application": "compatible",
        "schema": "manual_only",
        "dataLossRisk": true,
        "notes": "The previous application continues using the retained groups.currency field. Reversing the schema discards multi-currency settings and explicit accounting currencies."
      }
    },
    {
      "version": 37,
      "name": "add_user_feature_grants",
      "categories": ["additive", "constraint_tightening", "index"],
      "deployment": "online",
      "backfill": {
        "mode": "none",
        "resumable": false,
        "notes": "All users begin without grants, so the additive table requires no backfill."
      },
      "locking": {
        "risk": "low",
        "notes": "Creating an empty table and its index does not rewrite existing user rows."
      },
      "rollback": {
        "application": "compatible",
        "schema": "manual_only",
        "dataLossRisk": true,
        "notes": "The previous application ignores the additive table. Reversing the schema discards feature-grant state."
      }
    }
  ]
}
