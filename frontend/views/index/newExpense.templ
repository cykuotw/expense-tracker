package index

import (
	"expense-tracker/frontend/views/layouts"
	"expense-tracker/frontend/views/components"
)

templ NewExpense(groupId string) {
	@layouts.Base() {
		@components.Navbar() {
			@components.NewExpense(groupId)
		}
	}
}