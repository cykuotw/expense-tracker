package index

import (
	"expense-tracker/frontend/views/components"
	"expense-tracker/frontend/views/layouts"
)

templ NewExpense(groupId string, isSubmit bool) {
	@layouts.Base() {
		@components.Navbar() {
			@components.NewExpense(groupId, isSubmit)
		}
	}
}
